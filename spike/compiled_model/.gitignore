# Prebuilt LiteRT runtime + accelerator libs are not committed (extract from the
# PyPI `ai-edge-litert` 2.1.5 wheel for your platform). See README.md.
*.dylib
*.so
*.dll
.dart_tool/
pubspec.lock

# Local LiteRT source clone used for ABI/source verification (see RESULTS.md).
_litert_spike/
